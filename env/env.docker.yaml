AppPort: 8080

DBHost: postgres
DBPort: 5432
DBUser: docker_postgres
DBPassword: dockerPsqlPassword
DBName: appdb

MaxGraceStop: 60
##LogFile use %s formatting to set as a date (yyyy-mm-dd)
LogFile: 
LogLevel: info